<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 声明 handlerMapping -->
    <!-- 声明 BeanNameURI 处理器映射，其为默认的处理器映射 -->
    <!--    <bean id="beanNameUrlHandlerMapping"-->
    <!--          class="org.springframework.web.servlet.handler.BeanNameUrlHandlerMapping"/>-->

    <context:component-scan base-package="com.wind.spring.web"/>

    <mvc:annotation-driven/>

    <!--    拦截器注册-->
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/api/interceptor/userinfo"/>
            <!--<mvc:mapping path="/api/interceptor/*"/>-->
            <bean class="com.wind.spring.web.interceptor.LoginInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- InternalResourceViewResolver视图解析器，这个视图解析器一般会用来解析JSP视图 -->
    <bean id="viewResolver"
          class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/"/>
        <property name="suffix" value=".jsp"/>
    </bean>
</beans>